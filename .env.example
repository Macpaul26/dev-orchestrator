# Names only. Never commit values.
# Not required for Phase 1+2 - the walking graph is fully deterministic
# and makes no model calls.

# ANTHROPIC_API_KEY=
# GITHUB_TOKEN=

# Optional: override where checkpoints and run history live.
# ORCHESTRATOR_HOME=
